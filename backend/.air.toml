root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  delay = 1000
  exclude_dir = ["tmp"]
  include_ext = ["go"]
  kill_delay = "0s"

[watcher]
  poll = true
  poll_interval = 300

[log]
  time = false

[misc]
  clean_on_exit = true
